machine:
  environment:
    XCODE_SCHEME: SwiftLesson
test:
  override:
    - xctool -project SwiftLesson.xcodeproj -scheme SwiftLesson -sdk iphonesimulator -reporter junit:./test-result.xml clean test
    