machine:
  environment:
    XCODE_SCHEME: SwiftLesson
test:
  override:
    - xctool -project SwiftLesson.xcodeproj -scheme SwiftLesson -sdk iphonesimulator -report junit:./test-result.xml clean test
    